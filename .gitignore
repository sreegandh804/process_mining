# Cached raw corpus pulls and any large clones — reproducible via `python ingest.py`
/data/corpus/
/data/raw/
# Build output (regenerated by `python run.py`)
/out/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Editor / OS
.DS_Store
*.swp

# Secrets — never commit
.env
.env.*
